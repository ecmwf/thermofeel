version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

mkdocs:
  configuration: mkdocs.yml

python:
  install:
    - requirements: docs/requirements.txt
    # install thermofeel itself so mkdocstrings can introspect the docstrings
    - method: pip
      path: .
